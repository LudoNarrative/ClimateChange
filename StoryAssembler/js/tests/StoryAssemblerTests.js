/* global test */
"use strict";
define(["../StoryAssembler", "../ChunkLibrary"], function(StoryAssembler, ChunkLibrary) {
	
	var run = function() {
		test("story assembler core algorithm", function( assert ) {
			
		});
	}

	return {
		run: run
	}
});